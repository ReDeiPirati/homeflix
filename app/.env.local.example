# HomeFlix Environment Variables
# Copy this file to .env.local and adjust values as needed

# Path to media files (videos, images)
MEDIA_ROOT=/media

# Path to data directory (config files)
DATA_DIR=/data

# Path to library configuration file
LIBRARY_CONFIG=/data/library.yml

# LAN-only mode (no external access)
LAN_ONLY=true
